import { prop } from 'ramda';

export const propId = prop('id');
export const propTitle = prop('title');
export const propBody = prop('body');
export const propUserId = prop('userId');
export const propName = prop('name');
export const propEmail = prop('email');
